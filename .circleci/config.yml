version: 2
jobs:
  build:
    working_directory: ~/rocky/libcdio-paranoia
    parallelism: 1
    shell: /bin/bash --login
    environment:
      CIRCLE_ARTIFACTS: /tmp/circleci-artifacts
      CIRCLE_TEST_REPORTS: /tmp/circleci-test-results
    docker:
    - image: cimg/base
    steps:
    - checkout
    - run: mkdir -p $CIRCLE_ARTIFACTS $CIRCLE_TEST_REPORTS
    - run: sudo apt-get -y install libcdio-dev
    - run: sh ./autogen.sh
    - run: sh ./configure --enable-maintainer-mode
    - run: make
    - restore_cache:
        keys:
        # This branch if available
        - v2-dependencies-{{ .Branch }}-
        # Default branch if not
        - v2-dependencies-master-
        # Any branch if there are none on the default branch - this should be unnecessary if you have your default branch configured correctly
        - v2-dependencies-
    # Save dependency cache
    - save_cache:
        key: v2-dependencies-{{ .Branch }}-{{ epoch }}
        paths:
        # This is a broad list of cache paths to include many possible development environments
        # You can probably delete some of these entries
        - vendor/bundle
        - ~/virtualenvs
        - ~/.m2
        - ~/.ivy2
        - ~/.bundle
        - ~/.go_workspace
        - ~/.gradle
        - ~/.cache/bower
    # Test
    #   This would typically be a build job when using workflows, possibly combined with build
    # This is based on your 1.0 configuration file or project settings
    - run: make check
    # CircleCI doesn't have a pandoc in apt anymore.
    # https://stackoverflow.com/questions/59656257/trying-to-download-the-latest-version-of-pandoc-in-circleci-build-script
    # describes how to install pandoc, but this is too more of a hassle than it is worth.
    # - run: make GIT2CL=cat distcheck
    # Teardown
    #   If you break your build into multiple jobs with workflows, you will probably want to do the parts of this that are relevant in each
    # Save test results
    - store_test_results:
        path: /tmp/circleci-test-results
    # Save artifacts
    - store_artifacts:
        path: /tmp/circleci-artifacts
    - store_artifacts:
        path: /tmp/circleci-test-results
